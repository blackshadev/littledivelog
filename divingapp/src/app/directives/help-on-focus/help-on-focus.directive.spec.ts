import { HelpOnFocusDirective } from './help-on-focus.directive';

describe('HelpOnFocusDirective', () => {
    it('should create an instance', () => {
        const directive = new HelpOnFocusDirective();
        expect(directive).toBeTruthy();
    });
});
