import { DivetimePipe } from './divetime.pipe';

describe('DivetimePipe', () => {
    it('create an instance', () => {
        const pipe = new DivetimePipe();
        expect(pipe).toBeTruthy();
    });
});
